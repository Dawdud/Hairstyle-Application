import { ForbiddenValidatorDirectiveDirective } from './forbidden-validator.directive';

describe('ForbiddenValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
